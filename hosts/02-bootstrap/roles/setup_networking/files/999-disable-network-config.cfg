# This should go to /etc/cloud/cloud.cfg.d/99-disable-network-config.cfg
#
# Changes to etc/netplan/50-cloud-init.yaml are not persist across an instance reboot.
# To disable cloud-init's # network configuration capabilities, write a file
network: {config: disabled}
